# Betterleaks auto-loads .gitleaks.toml (or .betterleaks.toml) from the repo root.
# Kept as .gitleaks.toml for drop-in compatibility; extend built-in rules.
title = "tinkerway"

[extend]
useDefault = true
